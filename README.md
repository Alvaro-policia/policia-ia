# policia-ia

## Dependencias

- Python: `streamlit`, `openai`
- Node.js: `@openai/codex` (CLI)

## Instalación de Codex CLI

```bash
npm install -g @openai/codex
```

## Uso de Codex

```bash
codex "tu pregunta o tarea aquí"
```

Requiere la variable de entorno `OPENAI_API_KEY` configurada.
